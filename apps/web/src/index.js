const port = process.env.WEB_PORT || 3000;

console.log(`[web] placeholder application configured for port ${port}`);
console.log('[web] future pages: dashboard, profile, jobs, applications, billing');
