const port = process.env.API_PORT || 3001;

console.log(`[api] placeholder server configured for port ${port}`);
console.log('[api] business modules will be added here: auth, profiles, jobs, applications, autofill, ai, billing');
